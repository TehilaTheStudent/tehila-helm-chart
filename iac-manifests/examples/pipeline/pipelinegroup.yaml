apiVersion: pipeline.codearts.crossplane.io/v1alpha1
kind: PipelineGroup
metadata:
  name: example-pipelinegroup
spec:
  forProvider:
    region: cn-north-7
    projectIdRef:
      name: example-project
    name: ci

